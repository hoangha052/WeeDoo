//
//  EncoderExtension.swift
//  Bartr
//
//  Created by Thuy Luong on 6/16/16.
//  Copyright © 2016 Thuy Luong. All rights reserved.
//

import Foundation
//import Gloss
/*
extension Encoder {
    static func encodeInterests(key: String) -> [Interest]? -> JSON? {
        return {
            interests in
            if let interests = interests {
                var interestIdArray = [Int]()
                for item in interests {
                    interestIdArray.append(item.interestId!)
                }
                return [key: interestIdArray]
            }
            return nil
        }
    }
    
    static func encodeLocationId(key: String) -> Location? -> JSON? {
        return {
            location in
            if let validLocation = location,
                   validLocationId = validLocation.locationId {
                return [key: validLocationId]
            }
            return nil
        }
    }
    
    static func encodeTimeslotStatus(key: String) -> TimeslotStatus? -> JSON? {
        return {
            timeslotStatus in
            if let status = timeslotStatus {
                return [key: status.rawValue]
            }
            return nil
        }
    }
    
    static func encodeOfferStatus(key: String) -> OfferStatus? -> JSON? {
        return {
            offerStatus in
            if let status = offerStatus {
                return [key: status.rawValue]
            }
            return nil
        }
    }
    
    static func encodeTripStatus(key: String) -> TripStatus? -> JSON? {
        return {
            tripStatus in
            if let status = tripStatus {
                return [key: status.rawValue]
            }
            return nil
        }
    }
    
    static func encodeServiceType(key: String) -> ServiceType? -> JSON? {
        return {
            serviceType in
            if let type = serviceType {
                return [key: type.rawValue]
            }
            return nil
        }
    }

}
 */